import React from "react";

interface TextTapProps {
  handleChange: (e: React.ChangeEvent<HTMLInputElement>) => void;
}



const TextTab: React.FC<TextTapProps> = ({ handleChange }) => {
  return (
    <div></div>
  )
};

export default TextTab;
